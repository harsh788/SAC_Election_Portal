extend layout 

block content 
  h1= title 

  ul 
    each vote, index in vote_list 
      li #[strong #{student_list[index].name}] voted for #[strong #{candidate_list[index].full_name}] on #{vote.timestamp} 
